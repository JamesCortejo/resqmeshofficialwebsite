const SYNC_DEVICE_STATUSES = {
  ACTIVE: 'active',
  REVOKED: 'revoked'
};

const SYNC_DEVICE_PRINCIPAL_TYPE = 'mesh_node';
const SYNC_DEVICE_CLIENT_TYPE = 'device_sync';

module.exports = {
  SYNC_DEVICE_STATUSES,
  SYNC_DEVICE_PRINCIPAL_TYPE,
  SYNC_DEVICE_CLIENT_TYPE
};

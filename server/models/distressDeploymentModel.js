const DEPLOYMENT_STATUSES = {
  DEPLOYED: 'deployed',
  CANCELED: 'canceled',
  ACCOMPLISHED: 'accomplished'
};

const DISTRESS_FILTERS = ['all', 'unassigned', 'deployed', 'canceled', 'accomplished'];

module.exports = {
  DEPLOYMENT_STATUSES,
  DISTRESS_FILTERS
};
